# GetLogni V1

A modern dark technology/architecture homepage for GetLogni.

## Run locally

npm install
npm run dev

Open http://localhost:3000

## Production

This project uses Next.js static export. `npm run build` creates an `out/` directory suitable for the existing private S3 + CloudFront/OAC deployment.

Do not deploy to production until the local design and content are approved.
